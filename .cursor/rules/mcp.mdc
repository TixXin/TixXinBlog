---
description: Cursor 的项目 MCP 规则入口，使用仓库共享技能
alwaysApply: true
---

# MCP 工具规则入口

先读取根目录 [AGENTS.md](../../AGENTS.md)，再读取并遵循[共享 MCP 技能](../../.agents/skills/mcp/SKILL.md)。使用当前会话实际提供的工具及其 schema；不可用时使用已授权的可用工具，不固定旧工具名或仓库绝对路径。

Cursor 共享配置保留在 `.cursor/mcp.json`；`.cursor/mcp/memory.jsonl` 是已忽略的本机记忆文件。公共规则维护在共享技能与 AGENTS.md 中，不在此复制。
