---
description: Git 提交格式与授权的公共规范入口
alwaysApply: true
---

# Git 提交规范

读取根目录 [AGENTS.md](../../AGENTS.md) 的 Git Conventions 和[提交消息模板](../../.gitmessage)。使用中文 Conventional Commits 标题，空一行后添加解释变更原因的中文正文，不添加 AI 署名。

按当前会话授权检查差异和暂存范围，分模块验证并及时提交；保留他人改动，不重复询问已有授权。推送和部署遵循对应授权。

用户调用 `git` 命令时，按[共享 Git 技能](../../.agents/skills/source-command-git/SKILL.md)执行。
