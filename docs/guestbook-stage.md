# 开发数据保障与留言板业务阶段

基于实际工作区继续推进，起点为 `ade1001`。保留正在进行的样本正文规范化修改；测试数据身份只在内部标识和维护记录中体现，不污染用户可见正文。

| 模块               | 状态   | 验证依据 / 剩余                                                                                            |
| ------------------ | ------ | ---------------------------------------------------------------------------------------------------------- |
| 数据检查与目录     | 已实现 | `db:dev check-data`；隔离目录测试、实际网关身份及正常筛选空结果                                            |
| 增量样本与所有权   | 已实现 | core-v1共71条、guestbook-v1共38条归属已落日常库；七域覆盖、媒体互动、幂等、失败回滚、不复活及定向清理通过  |
| 留言数据库与API    | 已实现 | 88请求隔离集成、43项既有后端单测、Lint/类型与零结构漂移；同源网关已接入，见 backend/guestbook.md           |
| 留言前台与输入     | 已实现 | 真实聊天/论坛、发送/回复/回应、12项状态单测、三主题手机样本显示及主要三浏览器回归通过                      |
| 后台管理           | 已实现 | 列表筛选、回复、审核、置顶和删除；真实接口及隔离浏览器验证                                                 |
| 维护兼容           | 已实现 | v3内容包兼容v1/v2、回复映射、完整备份恢复、清理范围和外部互动保护通过                                      |
| 完整验收与日常交付 | 已完成 | 115前端/43后端单测、完整接口与数据测试、216联合浏览器回归、45留言最终复测、独立构建与日常数据/清理核对通过 |

本阶段已完成，完整结论与本地提交见[验收记录](guestbook-stage-verification.md)。日常样本与隔离数据严格分开，测试图片、trace与日志写入 `.artifacts/guestbook-stage/` 等本机忽略目录。实际开发服务当前仍为前端3456、API3000和PostgreSQL15433；任何数据库补齐必须先核对当前配置，不能依赖本文端口猜测目标。

实现边界：留言保留整数编号兼容原显示模型；公开/待审/隐藏与发送状态分开。回复是对单条消息的引用，隐藏父消息不自动删除其他公开回复，公开序列化必须移除不可见父消息内容。单条公开置顶由数据库部分唯一索引约束。回应沿用现有8种值，通过明细表按访客去重计数，不提供在线或已读假状态。

迁移前备份：`.backups/backup-1788952019396-eb398d4c/`；核心样本补齐前备份：`.backups/backup-1788952836374-59b64b5a/`。二者只保留本机。当前文章69条（公开66）、闪念40条（公开36）、朋友圈36条（公开34），包含此前日常记录，不能拿这些总数代替后续留言验收。

留言样本已增量写入日常库：29条、公开26条，公开列表首批20条、有后续游标，侧栏统计14名参与者、6名活跃成员和1条置顶。实际开发服务三主题在1600、390、320px均已显示真实列表和侧栏/抽屉，无横向溢出或页面错误。最终复核确认样本仍保留，七域检查通过；全部隔离验证资源已清理，开发服务继续运行。
