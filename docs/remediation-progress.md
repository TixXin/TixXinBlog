# 全项目整改进度

目标：逐项修复并验收 2026-09-06 审计的全部 28 项，包含真实认证/创作后台、数据源统一、依赖与交付；不以临时兼容或文档说明代替功能完成。

原始证据和建议见 [审计报告](project-audit-2026-09-06.md)。状态只有通过当前代码、自动化及必要运行验证后才能改为完成。

| 编号 | 优先级 | 项目                                   | 状态                           | 验证证据                                                                                        |
| ---- | ------ | -------------------------------------- | ------------------------------ | ----------------------------------------------------------------------------------------------- |
| 01   | P1     | JWT 密钥初始化早于环境文件加载         | 已验证                         | JWT 使用配置密钥并拒绝旧默认密钥；环境优先级回归、真实 HTTP 登录通过                            |
| 02   | P1     | 请求日志没有脱敏认证信息               | 已验证                         | 日志探针：Authorization/Cookie 明文均不再出现，存在脱敏标记                                     |
| 03   | P1     | MCP 配置含明文 API key                 | 已验证 | 2026-09-07 官方只读接口验证：旧密钥 HTTP 401，新密钥 HTTP 200；新值仅保存本机用户环境变量，配置与秘密扫描通过 |
| 04   | P1     | Docker 构建上下文未排除本地秘密文件    | 已验证                         | 生产镜像 smoke 通过：非 root、运行目录无本地环境及 MCP 文件；构建上下文排除规则与秘密扫描通过   |
| 05   | P1     | 代码块高亮失败时存在 HTML 注入路径     | 已验证                         | 生产浏览器未知语言回退显示原始文本，无 img/script 元素或弹窗；高亮失败安全边界单测通过          |
| 06   | P1     | 登录和公开写接口缺少应用层限流         | 已验证                         | 真实 HTTP 突发写入出现 429 + Retry-After；计数窗口测试通过                                      |
| 07   | P1     | Refresh token 轮换不是原子消费         | 已验证                         | 独立 PostgreSQL 中并发刷新同一 token 返回一个 200、一个 401                                     |
| 08   | P1     | 依赖安全告警较多且 CI 不阻断           | 已验证                         | corepack pnpm audit --prod --json：全部严重级别均为 0；CI 高危审计改为阻断                      |
| 09   | P2     | 公开闪念 feed 包含草稿和归档种子       | 已验证                         | 本地 RSS 与 JSON 均不再包含草稿/归档闪念                                                        |
| 10   | P2     | Sitemap 缺少文章详情，反而收录内部页面 | 已验证                         | 生产浏览器 E2E 验证发布收录、撤回移除，并排除管理/调试路由                                      |
| 11   | P2     | 主题调试页在生产环境公开可访问         | 已验证                         | 生产浏览器与容器 smoke 均验证主题调试页返回 404                                                 |
| 12   | P2     | 前端认证与后台管理没有真实闭环         | 已验证                         | 7 条生产浏览器流程通过；真实认证、文章创作和闪念管理覆盖，评论管理真实 HTTP 集成通过            |
| 13   | P2     | 文章周边仍与数据库使用不同数据源       | 已验证                         | 公开入口统一后端查询；发布撤回 E2E 通过；新增历史评论计数校正迁移并在本地执行                   |
| 14   | P2     | 文章超过 100 篇后会在前端列表中遗漏    | 已验证                         | 隔离数据库 106 篇文章，浏览器验证最后一页可达                                                   |
| 15   | P2     | 上一篇/下一篇写死                      | 已验证                         | 相邻链接由当前文章 API 生成，新文章导航不指向自身                                               |
| 16   | P2     | 文章点赞和浏览计数前端尚未接 API       | 已验证                         | 文章浏览计数及点赞经同源 API；浏览器点赞刷新保持并可取消                                        |
| 17   | P2     | 文章/闪念点赞与浏览仍可能丢失并发增量  | 已验证                         | 真实 PostgreSQL 集成验证文章与闪念的并发互动计数                                                |
| 18   | P2     | 闪念博主 ID 契约不一致                 | 已验证                         | 闪念真实身份、草稿/归档权限及编辑流程通过 HTTP 集成和浏览器 E2E                                 |
| 19   | P2     | 健康就绪探针不能反映依赖故障           | 已验证                         | 真实数据库连接关闭后 /ready 返回 503；单元测试通过                                              |
| 20   | P2     | 本地保存失败被吞掉                     | 已验证                         | 浏览器模拟存储容量异常：保留输入、恢复后重试成功；多键回滚单测通过                              |
| 21   | P2     | 导入数据缺少字段和 URL 协议校验        | 已验证                         | 导入字段/协议单测及浏览器未知版本拒绝且原数据保留验证通过                                       |
| 22   | P2     | 代码高亮缓存 key 会发生内容碰撞        | 已验证                         | 代码块改实例唯一 key，内容监听刷新；同前缀不同内容测试通过                                      |
| 23   | P2     | Dock 移动导航缺少可访问名称            | 已验证                         | 390px 三主题浏览器流程通过，Dock 导航可访问名称有断言                                           |
| 24   | P2     | CI 尚未覆盖真实端到端业务链路          | 已验证（远程 CI 待提交后运行） | 已配置隔离 HTTP/浏览器回归及容器 smoke 门禁；本机 7 条浏览器流程与容器 smoke 通过               |
| 25   | P2     | 运行时 API 地址与 CSP 使用不同配置时机 | 已验证                         | 容器中注入内部 API 地址，浏览器经同源网关登录读取；Cookie/CSP 和运行时站点地址验证通过          |
| 26   | P2     | 运行环境与发布检查基线不一致           | 已验证                         | Node 24 前后端与迁移镜像构建成功；故障与重启恢复 smoke 通过                                     |
| 27   | P3     | 仓库协作规则和阶段文档仍有冲突         | 已验证                         | MCP 路径、署名规则、Cursor 阶段说明、README、架构与目录说明已同步；生产部署说明已补齐           |
| 28   | P3     | 文章标题样式选择器不匹配实际 DOM       | 已验证                         | 生产浏览器断言 h2/h3 字号 20/18px 与间距生效；截图已检查；同时修复暗色高亮选择器                |

## 最终状态

2026-09-07：28 项整改均已完成本地验证。旧密钥已失效，新密钥可用。远程 CI 需提交后触发，本次未提交、推送或部署外网。以下执行记录保留历史过程，密钥状态以本节及第 03 项最终验证为准。

## 执行记录

- 2026-09-06：开始全部整改；首先处理安全配置、日志、秘密文件隔离与渲染，再推进数据、后台及完整验收。保留现有数据库和未提交改动。

- 当前已保留本地测试文章 ID 51 为草稿，原有 50 篇文章和 12 条测试评论保留。
- 最新前端单元测试：48 项通过；后端单元测试：19 项通过；真实 HTTP 集成脚本已通过（含闪念/评论管理与历史计数修复）。
- 当前开发进程：前端 exec 会话 82668、后端 exec 会话 85946；旧依赖升级会话已完成。

- 2026-09-06 收尾：去掉新建管理员默认密码回退；种子评论计数归零；历史计数迁移在本地执行且 schema drift 检查通过；隔离测试禁止写快照并清理 5 个历史临时快照。
- 容器 smoke 已实测通过并接入 CI；后端/迁移镜像已刷新，计数迁移与生产管理员初始化入口均通过最新容器 smoke。
- 最新后端验证：lint、类型检查、19 项单测、完整真实 HTTP 集成与计数迁移回滚/重放通过；测试结束未再产生源码快照。
- 新增 [生产式本地部署说明](local-production-validation.md)，包含独立卷、环境配置和不插入演示内容的管理员初始化命令。

- 浏览器新增旧格式文章专项：未知语言安全回退、同前缀代码块独立高亮、标题实际字号与间距、暗色主题关键字颜色。已核对截图；原演示文章正文保持不变，测试只写隔离库。

- 最终公开内容截图等待首屏加载层完全退出后采集；暗色代码颜色已验证。前端类型检查、48 项单测、lint 和生产构建通过。旧 Context7 密钥仍有效，账户侧撤销前不标记全部完成。

- 最终 7 条浏览器 E2E 全部通过（39.5s）；含最新高亮修复的前端镜像重建与容器 smoke 通过。本地开发两端已恢复，浏览器重新打开首页并展示真实 50 篇文章与 12 条评论统计。

- 重启恢复：新 Context7 密钥已保存到用户环境变量并验证 HTTP 200；旧历史密钥仍返回 200，无效密钥对照返回 401，仍待撤销旧值。未将新密钥写入仓库。
- 前后端改由可见 PowerShell 终端运行；重启后 Windows 保留 5432，开发 PostgreSQL 改映射 127.0.0.1:15433，后端 .env.local 同步。Compose 继续复用 server-main_pgdata；51 篇文章、12 条评论完整，首页和 /ready 验证通过。
