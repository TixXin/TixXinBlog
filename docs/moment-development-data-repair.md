# 朋友圈日常开发数据补正

日期：2026-09-09。

## 原因

上轮将朋友圈切换到真实 API，并在隔离库完成业务验证，但未给用户日常开发库补种。检查时 `/api/v1/moments` 与 `/moments/overview` 均请求成功，动态总数为0；照片墙、话题、日历和时间胶囊都依赖公开动态，所以一起为空。原前端演示数组仍在代码中，问题是开发数据交付遗漏，并非数据库记录被删除或侧栏组件丢失。

## 修复

- 使用现有开发数据脚本，核对本机 `127.0.0.1:15433/tixxin_blog`，先创建完整备份，再实际补入18条动态及18条评论。
- 首次导入的日期按今日UTC平移，保留原样本间隔，让近期日期组件有数据。重复执行不修改已有记录、互动或日期。
- `test:database-dev` 先构建独立于 `.dev-dist` 的测试运行产物，避免旧 `dist` 缺少新接口，导致测试使用过期后端。
- 将日常开发样本与临时自动化测试数据分开管理，所有后续功能开发必须配套并实际交付可浏览样本，要求已写入根目录 `AGENTS.md`。

执行命令：

```sh
corepack pnpm db:dev seed-moments
corepack pnpm db:dev seed-moments --apply --confirm tixxin_blog
```

写入前备份保留在本机忽略目录 `.backups/backup-1788946799819-741198ed/`。测试样本保留在日常开发数据库，不随隔离测试结束而删除。

## 验证

| 项目              | 结果                                                                           |
| ----------------- | ------------------------------------------------------------------------------ |
| 公开动态 / 评论   | 18 / 18                                                                        |
| 照片墙 / 热门话题 | 9张照片实际加载成功 / 5个话题                                                  |
| 日期与回顾        | 18个日期；当前9月3条动态；12条回顾候选                                         |
| 分页              | 第一页15条，第二页3条                                                          |
| 重复补种          | 新增0条，已有内容及日期不变                                                    |
| 三主题页面        | Chromium下 Nexus、Aurora、Dock 各1600/390px检查通过，侧栏和抽屉作者/日历各一份 |
| 自动验证          | 隔离数据库工具测试、后端Lint、类型检查、测试后端构建及 `dev:check` 通过        |

本轮浏览器检查只读日常开发数据；自动化写入和清空测试仍使用临时数据库。点赞为实际记录计数，当前种子不伪造点赞。截图、原始结果及日志位于本机 `.artifacts/moment-data-repair/`，未进入Git。本轮未重跑此前全部业务及三浏览器矩阵，因为前台业务代码未改变。

补种后对比备份：原51条文章、22条闪念、12条文章评论、站点配置及管理员记录摘要均未变化。隔离测试库及临时媒体已清理，日常开发样本保留。代码修复提交为 `f4970ec`；规范、修复记录及下一步提示词单独提交。

后续验收必须确认用户打开的开发页面已有代表性数据；只有种子脚本、只有隔离测试通过或只有空态可用，都不足以完成开发交付。完整下一步目标见[开发数据保障与留言板业务](next-goal-development-data-guestbook.md)。
