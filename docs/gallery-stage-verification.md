# 图库阶段验收

项目阶段补充复核：跨内容库旧编辑副本现完整只读保留；快速刷新和接管前原生历史导航发现的图库水合缺陷已修复。最新182项前端单测、117项图库与15项历史三浏览器全部通过，详见 [项目阶段验收](project-stage-verification.md)。

验收日期：2026-09-10。图库实现、日常样本、维护兼容、验证、清理与本地提交均已完成。图库117项三浏览器用例通过，相关既有功能516项回归也全部取得通过结果。项目、友链尚未在本记录中宣称完成。

## 实际交付与证据对应

| 要求 | 实现及权威验证 |
| --- | --- |
| 稳定作品与媒体关系、实际尺寸 | `gallery_photo` 外键关联 `media_asset`；图库90请求接口测试核对实际宽高/WebP，图库样本测试解码8张实图并核对HTTP访问 |
| 公开/管理边界、状态与时间 | 公开接口仅published且未删除；创建/拍摄/首次公开时间独立，未知拍摄信息保留缺省；接口测试拒绝伪造权限/尺寸/日期与越权管理 |
| 创建去重、版本冲突、删除与排序 | requestId/hash和删除墓碑，revision比较及媒体锁事务；接口并发、重试、排序、删除、撤回测试；浏览器实际完成创建到删除全流程 |
| URL、分页与灯箱 | 列表与服务端navigation按sortOrder/id排序；39项浏览器用例包含深链SSR、页码恢复、前进后退、慢请求关闭焦点、键盘切图与空态重试 |
| 后台媒体与输入保护 | 独立作品编辑页和器材配置；未知写入先查询提交状态，输入和副本按账号/内容库隔离；共享管理、上传和维护操作冻结发起身份，禁止跨账号重放 |
| 配置与统计来源 | 器材独立gallery_settings，日常默认空且不补假配置；分类、作品和地点仅统计公开记录；三主题各尺寸断言统计与器材可访问且只显示一份 |
| 日常样本与幂等 | gallery-v1：18作品、8图片、26归属；日常实际补齐后再次seed新增0保留26；隔离测试覆盖编辑保留、删除不复活、缺图诊断和跨业务引用保护 |
| 内容包与完整恢复 | v4显式gallery/gallerySettings，v1/v2/v3稳定兼容；预览确认、skip/copy、草稿迁入、映射、回滚、重复结果和配置变化测试；完整数据库/媒体及生产镜像恢复通过 |
| 清理及上下文 | 定向清理保留编辑/外部引用/账本/磁盘文件；clear-content和reset只在临时库验收；完整恢复旧令牌401、缺上下文428、旧上下文409、新上下文保存成功 |

## 日常开发目标

前端 `http://localhost:3456`；API `http://127.0.0.1:3000`；同源 `/api/v1`；数据库 `127.0.0.1:15433/tixxin_blog`。沿用既有配置与Docker卷，没有重置账号、站点设置或内容。

图库共18作品：16公开（12+4两页）、1草稿、1撤回；8张不同图片、26条新归属。公开分类为风景、城市、餐桌、人像和未分类，器材/地点未知的样本不虚构资料。原109条开发归属、文章/留言等内容仍保留。图片源素材在 `src/backend/server-main/src/seeders/gallery-assets/`，日常媒体存储在 `src/backend/server-main/var/media/`。

通过实际开发浏览器现有登录会话，只读查看 `/admin/gallery` 的18项列表、`/admin/gallery/8` 的标题/图片/拍摄日期/排序，以及 `/admin/gallery/settings` 的空配置与版本0；没有提交日常内容修改。前台实际三主题1600/390/320px读取12张首屏图片，媒体请求成功、无页面异常和横向溢出。图库专属的写入、冲突及删除验收全部使用隔离账号、数据库与媒体。

日常备份（本机长期保留）：

- `.backups/backup-1789044253574-73e4cf4a`：迁移前数据库与原2个媒体。
- `.backups/backup-1789044578014-89017e90`：图库补种前。
- `.backups/backup-1789045026660-65cdc3e2`：再次补种前，重复结果新增0、保留26。

## 已通过检查

- 后端90请求图库接口；完整既有后端11段接口链路；43项后端单测；迁移回放及零结构漂移；Lint与类型检查。
- 数据目录、核心样本、留言样本、图库样本、指定清理、数据库工具六组隔离回归，均正常退出。
- v4内容包、完整数据库/媒体恢复以及独立Node24 Alpine生产镜像的应用级恢复；镜像标签、恢复容器、卷及测试媒体已清理。
- 独立源码副本中前端生产构建、Lint、类型检查及32文件157项单测通过。单测覆盖图库状态、编辑/器材恢复、管理请求/媒体/内容包跨账号与代次保护。
- 图库最新版Chromium、Firefox、WebKit各39项，共117项通过。包含三主题×320、390、767、768、1023、1024、1279、1280、1439、1440px；320px减少动态效果；真实截图保留动态效果。

公共回归发现两处旧测试前置问题：闪念故障文案断言未覆盖当前准确提示，媒体测试在文件输入disabled时调用setInputFiles（该自动化方法可绕过浏览器原生禁用）。修订只精确核对已触发的目标故障及输入可操作条件，保留业务结果断言。Windows WebKit旧截图另需沿用已有字体请求/FontFace屏障；修正后blog三浏览器各24项全部通过，其他七个spec三浏览器444项已通过；最终相关回归合计516项。WebKit多部件route.fetch遗漏文件字节经独立对照确认，改为原生fetch真实201之后只丢失应用响应，完整保留强校验、票据和幂等断言。另修复原生批量弹窗取消焦点、以真实会话编号定位同名设备。

原始记录只保存在本机 `.artifacts/gallery-stage/`：`gallery-api.log`、`frontend-build-final.log`、`frontend-unit-final.log`、`browser-*-final.log`、`daily-*.png`、`daily-admin-*.png`、`docker-restore-{summary,report}.json`；浏览器截图和trace在其中的独立构建/回归子目录。Git仅保存测试源码、必要样本素材和文字结论。

本轮不代表真机触控、系统级移动浏览器、断电恢复或外部部署网络已经验证；未推送、部署或对外发布内容。友链申请、外部指标同步及书签云同步仍属后续范围。

最终清理核对：2026-09-10T13:54Z隔离数据库查询为空，结构漂移为空，日常图库仍18作品（16+1+1）及135条总归属；dev:check前后端均ready。所有专用测试进程、恢复容器/卷/标签、临时媒体与测试备份已经清理。最终状态保存在本机 `gallery-final-state.json`。
