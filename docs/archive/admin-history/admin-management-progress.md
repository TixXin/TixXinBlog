# 后台管理完善与验收

> 历史归档：原文来自 `docs/admin-management-progress.md`，保留当时的设计、实施与验收事实。文中的阶段、版本、数量和“当前／下一步”不代表现状；现行入口见[文档导航](../../README.md)。本机验收产物仍按原仓库相对路径留存。

目标：在现有 `/admin` 完成日常内容管理闭环。保留现有数据与未提交改动，不推送或外网部署。

| 范围     | 验收标准                                                             | 状态         |
| -------- | -------------------------------------------------------------------- | ------------ |
| 布局     | 概览、侧栏、顶部、面包屑、选中状态、移动端和明暗主题                 | 已完成并验证 |
| 概览     | 数据库真实数量、最近更新、待处理入口；管理员鉴权                     | 已完成并验证 |
| 文章     | 分类标签筛选排序分页、完整编辑、归档恢复、防重复提交与丢稿           | 已完成并验证 |
| 评论     | 按文章筛选、上下文、回复、级联删除说明与错误状态                     | 已完成并验证 |
| 闪念     | 后台列表编辑、状态与置顶、评论管理；公开隐私一致                     | 已完成并验证 |
| 分类标签 | 类型/专栏/标签定义、增改删、引用检查、迁移与公开同步                 | 已完成并验证 |
| 账号     | 会话恢复过期、退出、改密撤销旧会话、日志无凭据                       | 已完成并验证 |
| 验收     | 隔离数据库、浏览器正常/失败/移动端、lint/types/build/migration、文档 | 已完成并验证 |

现状：文章/评论管理与闪念管理 API 已存在。category 为 tech/life 固定内容类型，folder 为专栏名，tags 为多对多标签；专栏目录已持久化并完成存量迁移。概览“待回复”指公开文章下尚无博主直接回复的游客根评论，不是审核队列。

复用可见开发终端：前端 3456、后端 3000，开发数据库 15433；破坏性测试只用隔离库。

## 实施记录

- 2026-09-07：新增鉴权概览 API，数据库统计与最近更新；替换首页跳转为概览；增加侧栏、面包屑、选中项与主题切换。浏览器确认 51 篇文章、50 篇发布、1 篇草稿、12 条评论；匿名 API 返回 401。前后端 lint/类型检查通过。完整集成和移动端专项仍待后续验收。

- 2026-09-07：文章管理增加类型/专栏/标签组合筛选、排序、URL 保留与刷新恢复；列表显示标签、置顶和更新时间。归档恢复为草稿，写操作互斥、列表旧响应丢弃；编辑器新增刷新/关闭标签页未保存提示。浏览器验证技术+Vue3+升序返回 5 篇正确记录；隔离 HTTP 集成覆盖概览、筛选、无效排序、恢复鉴权与恢复后不公开并通过。前后端 lint/types 通过。完整生产浏览器回归在后续模块完成后执行。

- 2026-09-07：评论管理补齐文章筛选、待回复根评论、上下文祖先链与分页直接回复、删除完整数量；删除数量变化返回 409，防止确认期间新增回复被误删。新增回复输入离开保护与切换提示，失败保留内容；上下文和回复框打开后接收焦点。匿名权限、真实博主回复、待回复清除、范围变化拒绝、级联计数均通过隔离 HTTP 集成。浏览器检查现有三层评论的删除范围为 3，原数据未变。

- 2026-09-07：新增 `/admin/flashes` 与 `useAdminFlashes`，复用 FlashEditor 和管理员 API；接通全状态分页搜索、编辑草稿/发布、置顶、归档与恢复草稿、评论查看与删除。概览新增闪念和闪念草稿快捷卡片；编辑器图片/标签校验失败保留输入。真实 HTTP 集成已验证管理员详情权限与状态筛选。生产构建与完整浏览器回归正在执行，暂不标记最终验收完成。

- 本轮最终验证：前端生产构建通过，8 条浏览器回归全部通过（37.7s），包括后台闪念失败重试、草稿公开隔离、发布后 feed 同步、评论删除、置顶、归档与恢复后仍为草稿；现有公开闪念流程也通过。后台详情/状态筛选的真实 HTTP 集成通过。分类标签、改密及最终移动端/完整管理闭环仍待完成。

- 2026-09-07：专栏目录 post_folder 迁移及现存名称回填完成；本地 51 篇文章、12 条评论保持，6 个专栏覆盖全部文章（缺失 0）。新增 taxonomy 管理接口与页面，重命名同步文章/标签公开统计，引用中删除拒绝；编辑器可选择已有目录项。迁移无漂移、HTTP 集成、前后端 lint/types 通过。生产浏览器目录闭环仍在执行。

- 分类目录阶段：生产构建与 9 条浏览器回归通过（39.8s），含专栏/标签新建、文章引用、名称与颜色修改后 metadata 同步、刷新保留及引用中删除禁用。账号改密和最终移动端验收仍待完成。

- 2026-09-07：新增账号安全页与改密 API，session_version 迁移完成且无 schema 漂移；改密事务撤销所有 refresh、旧 access 即时失效。修复登录/刷新与改密的锁顺序，增加原地重新登录提示以保留编辑内容。隔离 HTTP 验证错误密码、改密/刷新并发、跨设备旧令牌拒绝、新密码登录与脱敏通过；后端 19 项单测通过。真实管理员密码未修改。生产浏览器改密测试运行中，最终移动端验收仍待完成。

## 最终验收（2026-09-07）

- 前后端 lint、类型检查及生产构建通过；前端 48 项单测、后端 19 项单测通过。
- 真实 HTTP 集成通过：概览鉴权、文章筛选与恢复、评论上下文/待回复/级联删除冲突保护、闪念状态、专栏标签引用与更名、改密并发及跨会话撤销、密码日志脱敏。
- 13 条生产浏览器流程通过（53.6s）。包含文章创作发布撤回、分类引用重命名、评论回复失败重试及删除、闪念完整管理、改密和原地重新登录保留草稿。
- 六个后台页面在 390px 下验证当前导航可见、无页面级横向溢出；文章列表改为移动端卡片。已检查概览、文章、评论、闪念、分类与明暗账号页截图。
- 两次迁移均已应用且 schema 无漂移；开发库仍为 51 篇文章、12 条评论、6 个专栏，真实管理员会话版本仍为 0，未修改真实管理员密码。
- 本地前端 3456、后端 3000 均可访问，继续复用可见日志终端。无提交、推送或外网部署。

范围说明：技术/生活为固定内容类型，专栏和标签可维护；封面与闪念图片通过 URL 配置。新密码只用于隔离账号的自动化测试。其他静态展示域及独立图片上传服务不属于本次目标。
