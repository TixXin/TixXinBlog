# 动效整改进度

目标：依据 motion-audit 的M01–M16完成正式实现与实际回归，保留三主题和业务能力。审查证据对应的338个源码指纹在整改开始时完全一致。整改前源码保存在 `.codex/motion-fixes/baseline/`，验证使用独立生成目录和随机数据库。

|任务|对应问题|状态|
|---|---|---|
|T01 零时长路由与布局状态解耦|M01|实施中|
|T02 侧栏取消/失败恢复|M02|实施中|
|T03 搜索按键与导航焦点|M03|实施中|
|T04 订阅所有权与水合偏好|M04、M09|实施中|
|T05 可选主题预热与启动恢复|M08|实施中|
|T06 可取消动效与减少模式|M05、M12|实施中|
|T07 颜色事务与即时降级|M07、M15|实施中|
|T08 轮播/视差与阅读滚动|M06、M10|实施中|
|T09 图像状态与Tooltip|M11、M13|实施中|
|T10 绘制性能与节奏|M14、M16|实施中|

## 必须完成的回归

三主题×正常/关闭/减少模式×30/120/300ms导航；后置守卫取消；搜索键鼠/IME/Esc；颜色100次及模态30次订阅账本；运行中减动效；列表/图片/请求错误；长文/目录/历史/滚动中断；嵌套弹层；CSS/SFC/布局/composable热更新与配置重启；启动及运行期主题资源失败；固定负载正常/节流至少3轮性能对照；多端断点与配色。

补充验证有效拖拽及键盘替代、Dock回顶、后台标签暂停、字体延迟、AI搜索和上下文分支。真实设备、系统键盘或特定浏览器不可用时提供明确证据与限制，不能伪造通过。

## 当前状态

正式实现已覆盖T01–T10，仍未将整轮标记为完成。以下阶段记录按先后保留，最终验收以最新源码对应的结果为准。临时服务使用可关闭的PTY，清理严格限定本次随机数据库与进程树。

## 已完成的阶段工作（待完整验收）

- 首批导航/侧栏/焦点/订阅修改通过局部Lint；独立前后端构建成功，见 `.codex/motion-fixes/build-foundation.log`、`build-motion-core.log`、`backend-build.log`。
- 两个真实composable中断回归在旧实现失败（旧颜色事务删新属性、列表卸载不收尾），新实现通过2/2，见 `motion-risk-before-final.log` 与 `motion-risk-after.log`。第一次测试缺少HappyDOM的ViewTransition接口，已补测试替身后重新记录旧实现失败。
- 108个样式文件增加各自作用域的reduce分支；正在接入JS/Sortable/图片。已有正常模式动效保留。
- 主题配置改为构建期元数据，当前主题并行预载；正在完善资源故障恢复。图库尺寸从本轮应用内浏览器实际naturalWidth/naturalHeight获取，保留原有长宽比例。
- 此阶段尚未启动隔离E2E；后续浏览器结果见下文。阶段构建或局部回归不能当作整轮通过。

## 浏览器阶段结果

- 已创建独立生产、开发和整改前生产预览；各自使用独立生成目录，单测另外使用frontend-test副本。服务由 `.codex/motion-fixes/serve.mjs` 管理，PTY句柄保持可关闭。
- 80项单测通过；阶段类型检查通过。新副本缺Nuxt生成类型的初始化问题已通过prepare解决，noscript位置调整后HappyDOM图标样式插入错误消失。
- 导航27组（3主题×3动效模式×30/120/300ms）全部通过，且无Vue水合/运行错误。
- 核心8项回归6项通过：取消导航2项、颜色100次/模态30次、运行中reduce、图片失败恢复、Tooltip退出。搜索不重开已经通过，但标题焦点最初提前落到旧main；已通过page:transition:finish修正，开发副本事件记录确认H1获焦，生产待重建复验。
- 功能10项中8项通过：三主题×两宽度长文共6项、Aurora暂停、灯箱/命令面板。发现书签内存数组未按已存排序字段排序、AI迟到结果覆盖新会话，已修正，待复验。
- Firefox与WebKit验证运行时已安装，尚未完成兼容矩阵。物理Safari/手机键盘仍需按实际能力记录边界。
- 目前全部问题仍保留最终验收状态；还有故障恢复、HMR、上下文弹层、跨浏览器与3轮性能对照等工作。

## 最新验证批次（2026-09-08）

- 路由视觉动效改由`usePageMotion`管理，NuxtPage保持稳定实例；正常模式仍有真实进入和离场，减少模式直接切换。生产27组导航压力、取消导航、搜索键鼠与IME、正常动画检查通过。
- 批次7：源码Lint、类型检查、80项单测及独立生产构建通过。隔离副本首次Lint误扫描了`.vite-motions`生成依赖；使用项目正式`pnpm lint`复查通过，生成代码诊断不列为产品问题。
- 完整生产E2E运行93项，90项通过、3项失败。1项产品缺陷：SSR图片在挂载前失败后未进入error状态；已补检测。2项测试数据假设错误：前序发布测试令总数从106增至110，连续加载测试仍写死106；已改为依据真实接口全集逐项比较ID及唯一性。
- 上述图片与连续加载复测，以及新增三主题真实滚轮中断回顶/运行中reduce收尾，共6项开发环境回归通过。原图加载失败被记录为产品缺陷；文章数量失败属于测试脚本，不宣称产品丢失或重复文章。
- 书签上下文菜单编辑后快速关闭，原焦点曾落入退场菜单。现在执行菜单动作前交还稳定书签入口，编辑框退出、分类及图标选择器测试通过。
- 字体延迟测试发现根组件快捷键不能inject其自身provide的搜索控制器；改为显式共享控制器。三主题字体请求有效延迟期间可操作，释放后搜索内容及焦点保留，Enter导航获焦均通过。
- 无动画颜色专项发现`transition: all`仍令滚动条颜色插值。已按用途收窄剩余58处all；普通反馈使用公共token，Vue过渡显式使用opacity/transform，评论展开保留max-height。7页面×有无VT接口共14项开发验证通过，颜色插值记录为空，系统颜色切换和末次事务清理正确。
- 启动入口、当前主题失败的只读内容/刷新恢复已通过并检查实际截图。开发服务器在其他会话访问主题后可能附带该主题的CSS，测试已区分附带样式与阻断启动的JS依赖，待最新生产构建重新完整验证。
- 最新HMR的CSS、SFC、布局、模态composable、共享偏好composable及配置重启6项通过。后台窗口实验仍报告visibilityState=visible，尚不能证明真实后台暂停；保留限制，不伪造hidden事件。
- 批次8正在以当前源码重新执行Lint/类型/单测/生产构建。跨Firefox/WebKit、多端矩阵、最终故障场景及正常/4倍CPU各三轮性能对照仍未完成。

## 收尾验证批次

- 批次9源码Lint、类型检查、81项单测与生产构建通过，完整隔离生产E2E **96/96通过**：`.codex/motion-fixes/e2e-runs/1788857185110-prod/report.json`。新增单测确认离屏列表项不创建入场任务；既有卸载收尾断言仍验证真实进行中的动画。
- 性能在相同106篇隔离数据、Chromium153、1440×1000、同一4秒操作序列中，整改前后各运行正常/4倍CPU三轮。设置遮罩的背景模糊经独立对照确认影响帧间隔，正式移除后p95约16.8ms；4倍CPU仍有200ms以上长帧，Paint总耗时未降低，不能宣称全面消除卡顿。完整分布见`evidence/performance-before/`、`performance-after/`；早期实现和单变量实验分别保留在`performance-after-batch8/`、`performance-after-no-blur/`。
- 三主题正常路由实际进入/离场已记录视频、连续帧与rAF动画状态：`evidence/normal-motion/`。三主题字体延迟生产验证通过；Hero离屏10.5秒和离开主页8.5秒不再轮播，通过`evidence/offscreen/results.json`。
- 响应矩阵75组中，Aurora768px先遇到定位歧义，进一步真实点击确认顶部导航被flex-end溢出裁剪；正式修改为可从起始位置横向滚动、空间充足时靠右。批次10已重新Lint/类型/构建；Aurora25组生产复验通过，证据`evidence/matrix-retry/`。其余50组见`evidence/matrix/`。
- 最新字体、颜色14项、资源故障3项、HMR6项均通过。资源故障截图等待离场稳定后检查，早期正在淡入的截图不冒充最终只读状态。
- 首轮Firefox/WebKit核心72项中68项通过，4项WebKit在交互断言通过后卡于截图等待。独立探针证明字体请求全完成、无loading FontFace，而document.fonts.ready不兑现；替代截图等待后原生图像可捕获。`tests/e2e/motionScreenshot.ts`以实际请求和FontFace状态等待，保持animations:allow，未修改应用字体或动画行为。
- 跨浏览器功能旧脚本将单次滚轮输入等同于固定像素距离，Firefox实际限制到约一屏；已用连续真实滚轮建立同样的长距离前置状态。用户滚轮的原生惯性也独立等待停止，再断言回顶被中断。旧WebKit等待测试已按验证过的进程身份终止并清理隔离库，原因保存在该次运行`cancelled.json`。
- 当前仍在完成修正后的跨浏览器回归、最终报告与资源清理；目标和todo保持进行中。

## 慢请求中断补充

- 批次12正式源码全套100项、Firefox/WebKit核心78项及功能30项均通过；75组最终响应矩阵与99张首页/设置截图已逐表核验。
- 补测慢文章请求尚未结束就转到全宽书签页，发现已销毁文章的AsyncData取消被当成fatal503，覆盖了新页面。现场URL是`/tabs`而正文为503，证据在`.codex/motion-fixes/evidence/slow-navigation/`；这是独立于零时长Transition的真实取消路径问题。
- 增加页面请求所有权和显式取消标记。读取随视图销毁中断，await之后停止旧初始化；默认布局只消费该预期取消，活动页面的真实503仍显示错误页。文章详情及补充信息合为一个AsyncData初始化任务，避免await之后再建立脱离页面的订阅。
- 评论仅取消初始读取，已有评论控制器的草稿、提交和重试仍由应用会话持有。新增回归要求取消在1.5秒内观察到，避免把原有10秒网络超时误当成导航取消。
- 慢请求三主题×三模式、详情/关联/评论读取取消及真实错误保留共39项三浏览器针对性回归通过；批次13重新Lint、类型检查、81项单测及生产构建通过，继续进行最终生产回归。
