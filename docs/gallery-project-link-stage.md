# 图库、项目与友链真实业务阶段

本轮按图库 → 项目 → 友链推进。每个模块必须完成持久化、公开及管理入口、带归属的日常样本、内容维护兼容、隔离验证和本地提交后再进入下一阶段。三个模块全部验收前不标记整体完成。

## 起点核对（2026-09-10）

- 开始时 Git 工作区干净；根目录 AGENTS.md 为唯一同名仓库指令文件。
- 文章、认证、评论、闪念、朋友圈、留言、媒体与站点设置已有真实实现，留言阶段已经验收；历史证据见 `guestbook-stage-verification.md`。
- 图库页面直接读取 `features/gallery/mock.ts`，照片数字编号、手填尺寸、分类与器材配置均为演示；现有筛选和灯箱需接入统一 URL 状态。
- 项目及友链仍为演示业务；书签继续 LocalStorage。本轮不扩展友链申请、外部指标自动同步或书签云同步。
- 初次 `dev:check` 与 `db:dev status` 因 Docker 引擎未运行导致数据库连接失败，不能据此判断内容为空。恢复既有 Docker 与 PostgreSQL 容器，保留原数据卷和配置。

## 交付约束

- 图库作品与媒体文件分离，宽高和格式来自媒体；拍摄日期缺省不冒用创建时间。
- 发布状态独立于项目进展、友链推荐；公开接口过滤非公开内容。媒体沿用既有文件访问边界。
- 管理操作复用认证、内容上下文、审计、提交去重、版本冲突和媒体引用锁。
- 内容包在当前 v3 基础上显式演进，保留旧版本契约；导入保持非公开。
- 日常库写入前确认本机目标并备份，只增量补齐；自动验证使用独立数据库、媒体和账号。
- 原始日志和截图只写入 `.artifacts/` 或 `.playwright-mcp/`，本文件记录实际文字结论。

## 当前状态

图库和项目已完成，友链实施中；阶段完成率2/3（66.7%）。最终证据见 [图库验收](gallery-stage-verification.md)和[项目验收](project-stage-verification.md)。

- 图库两条正式迁移已应用于日常 `127.0.0.1:15433/tixxin_blog`，迁移前完成数据库与两份原有媒体备份，结构零漂移。
- 日常 `gallery-v1` 已补齐18件作品（16公开、1草稿、1撤回）、8张本地图片、26条归属；重复补种新增0、保留26，无不可用项。媒体位于 `src/backend/server-main/var/media/`，原109条归属及既有内容保留。
- 日常备份保留 `.backups/backup-1789044253574-73e4cf4a`（迁移前）、`backup-1789044578014-89017e90`（补种前）、`backup-1789045026660-65cdc3e2`（重复补种前）。
- 后端图库90请求、既有完整接口链路、43项单测、Lint与类型检查通过。图库样本/清理/迁移重放等六组数据回归及v4内容包验证通过。
- 完整恢复既验证数据库与媒体摘要，也启动隔离Node24生产镜像，核对公开/后台图库、器材、媒体、重新登录与旧上下文拒绝。本次镜像、恢复容器、卷及测试媒体已清理。
- 图库前后台、深链分页、未知提交核查、公共401归属、器材和媒体输入保护均完成；独立构建、117项图库及516项既有功能三浏览器回归通过。
- 三主题日常公开页1600/390/320px读到12条首屏作品，无页面异常或横向溢出；现有已登录管理浏览器只读核对18作品及独立器材配置。

原始日志、截图、镜像恢复报告位于本机 `.artifacts/gallery-stage/`，不进入Git。已提交 `e0e819e`（阶段）、`e9b8c39`（图库API）、`4ac846e`（日常样本）、`3b1cf05`（v4维护）、`9b42c42`（深链定位）、`7a3ee08`（隔离浏览器与生产恢复）。图库使用说明见 [图库业务](backend/gallery.md)。

图库最终补充：157项前端单测、117项图库三浏览器和516项相关功能回归均通过，实际已登录开发浏览器的列表/编辑/器材入口只读核验通过。新增提交 `ec2b3c0`（请求归属）、`0b30e0b`（图库前后台）、`bd157f4`（能力与证据）、`4d90245`（跨浏览器收尾）。已确认隔离数据库为空、日常服务ready和原样本保留；开始项目阶段。

## 项目完成与友链开始

项目进展和发布状态独立，前台及全站搜索走真实API，后台可维护内容、封面、标签、链接与排序。日常18项目（16公开：8维护/4开发/4归档；1草稿、1撤回）、3媒体、21归属保留；重复seed新增0。全库现13媒体、156归属，原内容及图库仍在，三份项目迁移/补种备份与原图库备份长期保留。

79请求项目接口、完整13组后端回归、数据/清理/迁移零漂移、v5内容包兼容及独立生产镜像恢复通过；38文件182项前端单测、123项项目三浏览器、51项页面区域、6项恢复副本均通过。自然快速历史导航发现并修复图库水合缺陷，最新117项图库与15项历史三浏览器全部通过。2026-09-10T15:48Z最终复核测试数据库/媒体/备份/进程清理完成，日常服务ready。

提交 `1fa6d62`、`97fd816`、`0865b2b`、`45db987` 完成项目API、数据、维护与前后台；`5d7df7f`、`4cf6e37` 补齐旧编辑副本与图库水合。现进入友链阶段，继续完成真实维护、规则与本站资料、样本、v6兼容及三模块联合验收；公开申请、邮件、自动探活和抓取不在本轮实施。
